torch>=1.12.0
numpy>=1.21.0
pandas>=1.3.0
tqdm>=4.60.0

For parsing OWL files in the preprocessing script.
The paper does not specify a library, so we choose a standard one.
owlready2>=0.40

Dependencies for the Semantic Image Interpretation (SII) task
torchvision>=0.13.0
Pillow>=9.0.0